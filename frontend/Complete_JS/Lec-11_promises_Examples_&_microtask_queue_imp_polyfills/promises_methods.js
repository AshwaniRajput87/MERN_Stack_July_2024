/**
    promise methods:

     - resolve(value/data): success scenarios
     - reject(err): failure scenarios
     - then(scb, fcb): then is handling the rsolved value
     - catch(fcb): catch is hndling the reject errors.
     - finally(): it will make whether the promise has resolved or not, it has to be executed.

 */